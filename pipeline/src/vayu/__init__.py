"""VayuDrishti pipeline package.

Owns the frozen spatial/temporal conventions (grid, upwind, tz), all real-data
ingest modules, the feature-store builder, and the CLI subcommand registry.
"""

__version__ = "0.1.0"
