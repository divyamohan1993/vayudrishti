# local secrets (GEE service-account key path in .env.example points here)
.env
.env.*
!.env.example
secrets/

# data stores (rebuilt by vayu ingest/features; CI seeds from the data-seed release)
data/
*.parquet
models/artifacts/
pipeline/**/artifacts/

# build output (web static export is built by Vercel/Docker, never committed)
.next/
out/
web/out/
node_modules/
.vercel/
__pycache__/
.venv/

# verification and tooling artifacts
web/screenshots/
.playwright-mcp/
*.humanized

# OS cruft
.DS_Store
Thumbs.db
